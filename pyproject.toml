[project]
name = "anshlambda-dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Avinash Patel", email = "avinashjpatel19@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.12.3",
    "dbt-snowflake>=1.12.0",
]

[project.scripts]
anshlambda-dbt = "anshlambda_dbt:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
